html5 geolocation地理地位用于定位用户的位置

1、检测是支持地理定位
2、如果支持，则运行geolocation.getCurrentPosition()方法，如果不支持，向用户显示一条消息
3、如果getCurrentPosition（）运行成功，则向参数showPosition中规定的函数返回一个coordinates对象
4、showPosition（）函数获得并显示经度和纬度