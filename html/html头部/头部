一、head元素
    HTML<head>标签包含了所有的头部标签元素，在head元素中可以插入脚本、样式文件及各种meta信息
    可以添加在头部区域的元素为：title,style,meta,link,script,noscript,base
二、title元素
    此标签定义了不同文档的标题
    title在html、xhtml中是必须的
    title元素：
        定义了浏览器工具栏的标题
        当网页添加到收藏夹时，显示在收藏夹中的标题
        显示在搜索引擎结果页面的标题
三、base元素
    此标签描述了基本的链接地址、链接目标，该标签作为html文档中所有的链接标签的默认链接
四、link元素
    此标签定义了文档与外部资源的关系，通常用于链接到样式表
五、style元素
    此标签定义了html文档的样式文件引用地址
    也可以在此标签中直接添加样式来渲染html文档
六、meta元素
    meta元素描述了一些基本的元数据
    meta元素提供了元数据，元数据也不显示在页面上，但会被浏览器解析
    meta元素通常用于指定网页的描述，关键词，文件的最后修改时间，作者和其他元数据
    meta元素可以使用于浏览器，搜索引擎，或其他web服务等
    meta一般放置在head区域
七、