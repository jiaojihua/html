html多媒体标签：
    embed-定义内嵌对象
    object-定义内嵌对象
    param-定义对象的参数
    audio-定义声音内容
    video-定义视频内容
    source-定义dedia元素的多媒体资源（audio/video）
    track-规定media元素的字幕文件或其他包含其他文本的文件

为了使得多媒体文件被所有的浏览器支持，一种合理的方法是：
    source定义各种类型的多媒体文件
    然后使用embed标签
但是这种方法有一个隐患，那就是embed无法返回，即
如果它也播放不了多媒体文件，那么也不会出现错误提示了

